import SwiftUI

struct PublicWorkFilterView: View {
    var body: some View {
        VStack{
            Text("Public work filter")
            Spacer()
        }
        
    }
}

struct PublicWorkFilterView_Previews: PreviewProvider {
    static var previews: some View {
        PublicWorkFilterView()
    }
}
